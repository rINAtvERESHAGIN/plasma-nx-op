import styled from '@emotion/styled';
import Menu from '@mui/material/Menu';
import { Typography } from '@mui/material';

export const StyledMenu = styled(Menu)`
    z-index: 10000;
`;

export const RegionName = styled(Typography)`
    text-align: center;
    margin: 8px;
`;
