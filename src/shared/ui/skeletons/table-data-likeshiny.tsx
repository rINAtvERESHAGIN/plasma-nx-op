import React from 'react';
import { Skeleton } from '@mui/material';

const SkeletonLikeShinyTable = (): React.ReactNode => {
  return <Skeleton variant="rounded" width="100%" height={300} />;
};

export default SkeletonLikeShinyTable;
