import React from 'react';
import { Skeleton } from '@mui/material';

const SkeletonLikeShinyChart = (): React.ReactNode => {
  return <Skeleton variant="rounded" width="100%" height={300} />;
};

export default SkeletonLikeShinyChart;
